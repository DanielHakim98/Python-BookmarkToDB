#Project HTML to Python

1. This project focuses on to load HTML file in Python and store it into a Dataframe.
2. Steps:
   - Load "bookmarks.html" file into Python
   - Process the file content (Sorting,Grouping,Cleaning)
   - Store it in List/Dataframe
3. Now, Lets focus on the first steps, Loading the file. The thing need to be considered is what data I want to store and 
   the way I want to store it.
   - Store it in list of dictionary
   - In dictionary only contains 'id','title','topic','link'
4. The steps for loading is complete. Now there are two lists for title and links.